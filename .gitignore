# Local backlog / scratchpad — not tracked in the repo
PROGRESS.md
Progress.md

# Internal dev notes / planning docs — local-only, keep the public repo lean
CLAUDE.md
BUILD_SCHEMA_V2_PLAN.md
CHARACTER_BONUSES_PLAN.md

# GitHub Pages requires .nojekyll to serve this vanilla-JS site verbatim
# (without it the Jekyll build fails). Never ignore it.
!.nojekyll

# Source CSVs for tools/build-data.mjs — local-only; the built data/data.js is what ships
data/references/

# Generated build index (tools/build-index.mjs) — not needed until builds ship
builds/index.json

# Windows-generated per-folder view settings (System+Hidden); never our files
desktop.ini
